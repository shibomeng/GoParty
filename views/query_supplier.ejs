<% include ./partials/header %>

<header class="container">
    <div class="jumbotron">
        <h1 style="color: white;">Search Supplier</h1>
        <form action="/query_supplier" method="POST">
            <div class="form-group" id="Name">
				<select name='Supplier_Name' class="form-control custom-select">
					<option disabled selected>By Supplier Name</option>
					<%if (locals.supplier) {%>
						<%supplier.forEach(function(m) {%>
							<%for (var key in m) {%>
								<option value="<%= m[key] %>"><%= m[key] %></option>
							<%}%>
						<%});%>
					<%}%>
				</select>
            </div>
            <div class="form-group" id="Name">
				<select name='good' class="form-control custom-select">
					<option disabled selected>By Goods</option>
					<%if (locals.goods) {%>
						<%goods.forEach(function(m) {%>
							<%for (var key in m) {%>
								<option value="<%= m[key] %>"><%= m[key] %></option>
							<%}%>
						<%});%>
					<%}%>
				</select>
            </div>
            <div class="form-group">
                <button class="btn btn-primary btn-lg">Search</button>
            </div>
        </form>
    </div>
</header>

<div class="container" id="data">
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Client ID</th>
                <th scope="col">Budget</th>
                <th scope="col">Phone Number</th>
                <th scope="col">Address</th>
            </tr>
        </thead>
        <tbody>
                <%if (locals.info) {%>
                    <%info.forEach(function(m) {%>
                        <tr>
                            <%for (var key in m) {%>
                                <td>
                                    <%=m[key]%>
                                </td>
                            <%}%>
                        </tr>
                    <%});%>
                <%}%>
        </tbody>
    </table>
</div>

<% include ./partials/footer %>